import styled from 'styled-components'

export const Input = styled.input`
    border: 1px solid #eee;
    padding: 10px 15px;
    margin-right: 16px;
    font-weight: 800;
    color: #333;
    border-radius: 3px;
    height: 40px;

    &::placeholder {
        font-weight: bold;
    }
`